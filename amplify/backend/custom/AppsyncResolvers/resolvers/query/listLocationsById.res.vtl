#if($ctx.error)
    $util.error($ctx.error.message, $ctx.error.type)
#end

#set($items = [])
#foreach($i in $ctx.result.items)
    $util.qr($items.add($i.DATA))
#end
$util.toJson({"items": $items, "nextToken": $ctx.result.nextToken})